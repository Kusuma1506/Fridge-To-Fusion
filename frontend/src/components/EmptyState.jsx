export default function EmptyState({ searched }) { return <section className="state-card empty"><span className="state-icon">{searched ? '⌕' : '🥕'}</span><h2>{searched ? 'No recipes found.' : 'Enter ingredients to generate recipes.'}</h2><p>{searched ? 'Try adding a few more ingredients and search again.' : 'Start with what you already have—one ingredient is enough.'}</p></section>; }
